- name: install and run nginx
  hosts: web
  tasks:
  - name: install nginx package
    ansible.builtin.package:
      name: nginx
      state: present
  - name: run nginx
    ansible.builtin.services:
      name: nginx
      state: started